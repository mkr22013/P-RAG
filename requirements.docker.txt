# Runtime Python requirements for the P-RAG Docker image.
# Versions are pinned to the minor release for reproducibility.
# ──────────────────────────────────────────────────────────────

# LLM client (talks to ollama container)
ollama>=0.4,<1.0

# PDF parsing — medical & dental indexers
pdfplumber>=0.11,<1.0

# PDF parsing — SBC (Summary of Benefits and Coverage)
docling>=2.0,<3.0

# FastMCP server (imported in-process by client.py)
fastmcp>=2.0,<4.0

# Env file loading
python-dotenv>=1.0,<2.0

# FastAPI server (main.py REST API)
fastapi>=0.109,<1.0
uvicorn>=0.27,<1.0
python-multipart>=0.0.6

# PDF generation endpoint (/download-pdf)
fpdf2>=2.7
